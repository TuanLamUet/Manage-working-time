{
  "type": "mysql",
  "host": "localhost",
  "port": "${DB_PORT}",
  "username": "${DB_USERNAME}",
  "password": "${DB_PASSWORD}",
  "database": "${DB_NAME}",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true
} 